[workspace]
resolver = "3"
members = ["arc_mutex", "implement_future","mpsc_canal", "oneshot_and_timeout","runtime_and_async", "select", "spawn_and_join", "spawn_blocking", "tcp_server", "try_join_and_try"]

[workspace.dependencies]
tokio = { version = "1", features = ["full"] }
