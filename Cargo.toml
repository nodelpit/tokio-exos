[workspace]
resolver = "3"
members = ["mpsc_canal", "oneshot_and_timeout","runtime_and_async", "select", "spawn_and_join", "try_join_and_try"]

[workspace.dependencies]
tokio = { version = "1", features = ["full"] }
